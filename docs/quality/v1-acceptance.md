# V1 Acceptance Matrix

Evidence levels are deliberately separated: static/code, automated local, real PostgreSQL integration, provider mock, and external real-world validation are not interchangeable.

| # | V1 success question | V1.1 requirement IDs | Current evidence | Status |
|---|---|---|---|---|
| 1 | Child willingly uses it for seven days | `REQ-V1-CORE-01` | PostgreSQL records completed activity separately from Student-only willingness reflections; Owner reporting requires a historical seven-day window where every day has both. Role and database tests prevent Parent substitution and active-session reflections. No real child trial has been run | Instrumented; external validation required |
| 2 | Wrong answer produces root-cause diagnosis | `REQ-V1-CORE-02` | Private `answer_analyses`, misconception records, Parent projection, Tutor decision tests | Engineering pass |
| 3 | Three failed Socratic rounds deescalate | `REQ-V1-CORE-03` | Tutor unit tests and full E2E prove `PROBE -> SCAFFOLD -> ANALOGY -> VOICE_EXPLAIN` | Pass |
| 4 | Voice explanation returns to original verification | `REQ-V1-CORE-04` | Full E2E rejects direct answers in `VOICE_EXPLAIN`, requires explicit `/voice/complete`, keeps the original released question active, enters `RETURN`, and then requires a fresh correct submission; TTS protocol is provider-mock tested | Engineering pass; real provider pending |
| 5 | Cross-subject gaps remediate and return | `REQ-V1-CORE-05` | PostgreSQL+HTTP tests cover both Planner remediation and Tutor-diagnosed automatic `BACKTRACK`: they choose a released dependency, preserve the original task, complete the prerequisite, emit `CROSS_SUBJECT_INSIGHT`, restore the released original question in the same session, and require its correct answer | Pass |
| 6 | Student network responses cannot obtain answers | `REQ-V1-CORE-06` | Real PostgreSQL authenticated HTTP test plus Student event and bundle scans | Pass |
| 7 | Parent sees complete live state | `REQ-V1-CORE-07` | Bound Parent REST projection, evidence-derived mastery score, role-specific realtime fields, active-child discovery, persisted session/answer/Tutor/voice/mastery/reward/plan lifecycle events, answer-free ability/activity reports, and full E2E | Pass |
| 8 | Parent adjusts plans but cannot answer | `REQ-V1-CORE-08` | Preferences/intervention tests, first-intervention preference upsert, rejected `answer` field, and 404 assertion for send-answer-like route | Pass |
| 9 | Misconceptions recur across days | `REQ-V1-CORE-09` | Real PostgreSQL tests `TestB2ReviewQueueAssistedThenIndependentLifecycle`, `TestB2ReviewQueueFailureRollbackAndConcurrentCompletion`, `TestB2ReviewSessionStartRequiresBoundPendingItem`, and `TestB2ReviewQueueMigrationDeduplicatesAndBindsDeterministically` prove due misconception/review items are bound to the exact session, closed with assisted or independent results, rescheduled by the rules engine, rolled back atomically on failure, and protected against concurrent completion | Pass |
| 10 | Planner changes tomorrow from performance | `REQ-V1-CORE-10` | Full E2E verifies a replaced/proposed next-day plan based on session mastery | Pass |
| 11 | Every provider call has measurable cost | `REQ-V1-CORE-11` | Provider billing identity is mandatory; request-time price preflight blocks unpriced AI/STT/TTS before network calls; effective-date catalog tests, exact decimal calculation, response-before-validation recording, TTS latency/version transaction, metered TTS E2E, and Owner reports cover the accounting path | Engineering pass; real provider pending |
| 12 | Generated content must pass release gate | `REQ-V1-CORE-12` | PostgreSQL trigger rejects skipped states; executable Owner pipeline covers validation/review/provenance/release/quarantine; `MATH-LINEAR-EQUATION` repair assets pass the taxonomy-aware deterministic Validator and a deterministic secondary review with explicit five-dimension findings before re-release | Pass |
| 13 | Complete learning works at 320 px | `REQ-V1-CORE-13` | Playwright `e2e/student-classroom-320.spec.ts` (`student classroom at 320px with a soft-keyboard viewport`) drives real headless Chromium against Student-role authenticated-route fixtures and intercepted API responses, not a real server or PostgreSQL, at a 320 px viewport with reduced motion. It checks a deterministic 360 px low `visualViewport` approximation for keyboard obstruction, verifies task text plus free input, submit, hint, "我不会", and voice controls remain horizontally overflow-free and vertically reachable, confirms keyboard-close viewport and layout restoration, and scans Student-visible text for private-answer, full-solution, parent-score, and peer-comparison canaries that the intercepted responses genuinely carry, with a guard that fails if the injection is ever dropped. Server-side authentication and Student non-disclosure remain the responsibility of the real PostgreSQL integration tests `TestPasswordLoginUsesSecureHttpOnlyCookieAndServerSideRevocation` and `TestPostgresStudentAnswerNonDisclosureGate`. Headless Chromium cannot summon an OS IME; a real-device run remains required for IME animation, candidate bars, and platform-specific behavior | Pass (reproducible Playwright evidence with intercepted fixtures; real-device IME behavior pending) |
| 14 | Session lifecycle and active-time accounting match V1.1 | `REQ-SESSION-01`, `REQ-SESSION-02`, `REQ-SESSION-03`, `REQ-SESSION-04` | Real PostgreSQL lifecycle tests cover active-only timing, idempotent transitions, 90-second stale recovery, 24-hour abandonment, and the single unfinished-session constraint; frontend tests cover the 45-second interaction-freshness gate for keyboard, pointer, scroll, and voice activity, heartbeat stop/restart, explicit-only resume, and visibility/pagehide fast-path pauses. Production code checkpoints stale recovery at `last_activity_at`; final B2.1 test deployment must still confirm that an approximately two-minute idle interval is excluded from accumulated time. `TestB7LinearEquationLoaderIsIdempotentAndUpgradesExistingLineage` additionally proves the existing READY lineage upgrades in place without creating a second READY lineage and preserves the old content version. B7-7 adds `TestClassroomTimeoutOrderingKeepsRequestsAheadOfProxyAndStaleRecovery`, `TestB7InFlightSubmitIsExcludedFromStaleRecovery`, and `TestB7SubmitTimeoutReleasesLeaseAndRetryIsNotDeadlocked`: in-flight leases are active without counting provider wait as learning time, and timeout/cancellation cleanup leaves the classroom retryable. B7-8 separates the three reported symptoms: 504 is covered by the B7-7 75-second server budget and 85-second proxy ordering; false PAUSED is covered by `TestB8TextSubmitInFlightFreshnessDoesNotPauseAndHeartbeatDoesNotCountProviderWait`, `TestB8StructuredSubmitInFlightFreshnessDoesNotPause`, and frontend `keeps an in-flight submission alive after the interaction freshness window`, while `TestB8StaleRecoveryStillPausesStudentWhoActuallyLeft` preserves the legal idle pause. Visibility/pagehide uses deferred pause (方案 a), covered by frontend `defers pagehide pause until an in-flight submission closes`; legal pause recovery and lease cleanup are covered by `TestB8VisibilityPauseWithInFlightTextSubmitResumeClearsLease`, `TestB8VisibilityPauseWithInFlightStructuredSubmitResumeClearsLease`, `TestB8CancelledSubmitThenResumeIsNotDeadlocked`, `TestB8StructuredCancellationAndTimeoutThenResumeAreNotDeadlocked`, `TestB8ServerTimeoutThenResumeIsNotDeadlocked`, `TestB8ResumeAndSubmitCloseInEitherOrderLeaveOneActiveLeaseFreeSession`, and frontend `explicit resume after a legal pause restores controls without losing the draft`. `TestClassroomSubmitBudgetMatchesFrontendInFlightContract` guards the mirrored 75-second submit budget against server drift; the client freshness guard is explicit, and heartbeats during an active operation do not advance `last_activity_at`, so provider wait remains excluded from learning time | Engineering evidence; real idle-time validation pending |
| 15 | Runtime plan, streak, cache, and preference contracts match V1.1 | `REQ-PLAN-BLOCK-01`, `REQ-STREAK-01`, `REQ-API-CACHE-01`, `REQ-PARENT-PREF-01` | `TestPlanBlockStatusFollowsSessionLifecycle` proves Student-visible plan blocks transition `AVAILABLE -> ACTIVE -> COMPLETED` through the session lifecycle. `TestGrowthAndParentReportDeriveTheSameLiveStreak` and `TestShanghaiLearningDateBoundaryUsesPostgresDerivedActivityDate` prove streaks are recomputed from `student_activity_days` using Asia/Shanghai learning dates, including a real boundary split validated by `scripts/assert-timezone-date-split.sh Pacific/Kiritimati`. `TestPasswordLoginUsesSecureHttpOnlyCookieAndServerSideRevocation`, `TestPostgresStudentAnswerNonDisclosureGate`, `TestPlanBlockStatusFollowsSessionLifecycle`, `TestB1AGradeBandUnavailableParentSubjectsAreExplicit`, and `TestFourStageContentPreflightAndRuntimeDriftFailClosed` assert `Cache-Control: no-store` on representative 401, 404, 409, 422, and 503 API routes; the same login test covers successful and 204 API responses, while `TestAPIResponsesDisableCaching` covers the router-level 404 policy. `TestB1DParentPreferencesIgnoreYesterdayPausedSession`, `TestB1DParentPreferencesRebuildTodayWithoutSessions`, and `TestB1DParentPreferencesPreserveStartedTodayPlan` prove `plan_updated`, `today_preserved`, and Shanghai-date `applies_from` semantics | Pass |
| 16 | Teaching stages obey the frozen number policy | `REQ-TEACH-01`, `REQ-TEACH-02`, `REQ-TEACH-03` | `tutor-number-material-v1` rejects substituted numeric material in `PROBE`/`HINT`/`SCAFFOLD`/`ANALOGY` after the existing disclosure audit and before classroom persistence, realtime, or TTS. `EXPLAIN`/`VOICE_EXPLAIN` remain the only different-number example actions, and a server-authored prompt returns the Student to the original task. Unit and real PostgreSQL tests cover all bounded actions and the no-mutation boundary | Pass |
| 17 | Parent visibility follows realtime, report, and risk boundaries | `REQ-PARENT-VIS-01`, `REQ-PARENT-VIS-02`, `REQ-PARENT-VIS-03` | `TestB6ParentLivePreviewAndCompletedReportEnforceVisibilityBoundary` proves the server permits only an active, single-line, 80-Unicode-character current preview and withholds long, multiline, and non-active answers. `TestB6ParentRealtimePayloadNeverPersistsStudentAnswerBody` proves Parent realtime payloads carry no answer body. Completed Student/Tutor turns are server-produced summaries; frontend tests prove live preview and report-safe rendering, while B3 safety summaries remain minimal and access-audited | Pass |
| 18 | Growth uses explainable learning evidence rather than energy | `REQ-GROWTH-01`, `REQ-GROWTH-02` | `growth-evidence-v1` returns all five primary indicators with event-level source type/ID, subject, knowledge point, stage/form, and time. `TestB6GrowthIndicatorsExposeDeterministicEventProvenanceWithoutBodies` proves every class is derived from deterministic evidence and no answer body is serialized; Student and Parent frontend tests make these indicators primary while energy remains compatibility-only | Pass |
| 19 | Strict mastery semantics preserve independent evidence | `REQ-MASTERY-01`, `REQ-MASTERY-02`, `REQ-MASTERY-03`, `REQ-MASTERY-04`, `REQ-MASTERY-05` | Mastery engine unit tests prove `REQ-MASTERY-01` through `REQ-MASTERY-04`; real PostgreSQL `TestB7LinearEquationAssistanceRequiresFreshIndependentTaskAndFailsClosedOnDrift` activates `MATH-LINEAR-EQUATION`, excludes the presented task after HINT, requires a fresh deterministic task, and proves assisted success does not increment independent evidence counters | Pass for `REQ-MASTERY-01` through `REQ-MASTERY-04`; `REQ-MASTERY-05` Pass (B7-2 activation) |
| 20 | Assistance levels separate cognitive help from emotion breaks | `REQ-ASSIST-01`, `REQ-ASSIST-02` | Unit test `TestB2AssistanceLevelsKeepBreakNonCognitive` covers all eight mappings; real PostgreSQL test `TestB2BreakFollowedByCorrectAnswerRemainsIndependent` proves BREAK does not contaminate independent evidence | Pass |
| 21 | Planner never crosses the student's grade boundary | `REQ-GRADE-01` | Real PostgreSQL tests `TestB1AGradeBandPlannerModesAndFiveSubjects`, `TestB1AGradeBandStudentReadsStartsAndReturns`, `TestB1AGradeBandBacktrackNeverMovesUp`, `TestB1AGradeBandUnavailableParentSubjectsAreExplicit`, and `TestB1AGradeBandMigrationRemovesOnlyInvalidAvailableBlocks` cover grades 1/6/7/9, five subjects, asymmetric modes, direct reads, session boundaries, fallback, and stored plans; the three existing cross-subject planning/return tests now use a lower-band prerequisite | Pass |
| 22 | Cross-day paused sessions do not misreport plan effective dates | `REQ-PLAN-EFFECT-01` | Real PostgreSQL tests `TestB1DParentPreferencesIgnoreYesterdayPausedSession`, `TestB1DParentPreferencesRebuildTodayWithoutSessions`, and `TestB1DParentPreferencesPreserveStartedTodayPlan` independently cover yesterday-paused, no-session, and today-started boundaries | Pass |
| 23 | Successful review attempts close and reschedule deterministically | `REQ-REVIEW-01`, `REQ-REVIEW-02` | Real PostgreSQL tests `TestB2ReviewQueueAssistedThenIndependentLifecycle`, `TestB2ReviewQueueFailureRollbackAndConcurrentCompletion`, `TestB2ReviewSessionStartRequiresBoundPendingItem`, and `TestB2ReviewQueueMigrationDeduplicatesAndBindsDeterministically` cover exact-item binding, assisted/independent/failed history, rules-engine rescheduling, due-time planning, rollback atomicity, concurrency, start validation, and deterministic migration deduplication | Pass |
| 24 | Student-visible output fails closed on answer disclosure | `REQ-AI-AUDIT-01` | `TestB3BTutorOutputReviewRejectsBeforeClassroomRealtimeAndVoiceMutation`, `TestB3CReviewerRejectsSupportWithMinimal422AndNoClassroomMutation`, `TestB3BInvalidReviewSchemaFailsClosedAfterPricedUsage`, `TestB3BDeterministicGateRejectsSelfReportedSafeAnswerAfterIndependentReview`, `TestB3BUnpricedReviewerIsBlockedBeforeNetworkAndFailsClosed`, `TestB3BReviewer429RetryRecoversWithSinglePricedUsage`, and `TestB3BReviewerRetryExhaustionReturnsChildSafe503WithoutMutation` use real PostgreSQL and provider HTTP mocks to prove the independent dual gate, priced usage, strict enum/index-only violation persistence, content-free 422 rephrase responses at both classroom entry points, reviewer-only bounded retry, child-safe exhaustion, no classroom/realtime/TTS mutation, and deterministic rejection despite `answer_revealed=false`; unit, schema, and serialization tests cover six inconsistent-violation classes, provider-compatible structured output, missing/same-identity/timeout/invalid-provenance reviewers, 429/5xx/4xx/schema/rejection retry boundaries, `Retry-After`, operation deadlines, strengthened disclosure prompts across all four generation methods, every text segment, separated private reviewer input, and Student non-disclosure; frontend tests prove distinct answer/HINT rephrase prompts and the reviewer-unavailable retry prompt restore controls, preserve Student input, and keep existing 404/409 behavior | Pass |
| 25 | Child safety policy is versioned, minimal, and auditable | `REQ-SAFETY-01`, `REQ-PARENT-VIS-03` | `minor-safety-v1` covers the eight frozen categories with fixed severity/action/fallback rules before Teaching Agent input. Matched text is absent from answer, analysis, transcript, safety, and realtime records; classification cannot mutate mastery/reward/review/plan state. Student UI explains Parent escalation, non-escalated events are Parent-suppressed, escalated Parent fields are minimal, and every summary access appends an independent audit record. Unit, frontend, and real PostgreSQL tests cover all categories and role boundaries. The deterministic classifier is explicitly narrow and does not claim complete language coverage | Engineering pass; policy recall monitoring required |
| 26 | A classroom really completes variant, abstraction, and verification | `REQ-CLASSROOM-01`, `REQ-EVIDENCE-01` | Real PostgreSQL `TestB7LinearEquationActivationCompletesFourStagesWithPipelineEvidence` activates the audited `MATH-LINEAR-EQUATION` lineage, persists `ORIGINAL -> VARIANT -> ABSTRACT -> VERIFY -> COMPLETE`, records LIFE/VARIANT/TEXTBOOK/TEXTBOOK evidence from completed stages, and proves the all-correct path makes zero AI calls; `TestB7LinearEquationParentReceivesReadableAnswerButStudentDoesNot` proves the repaired parent answer path without Student disclosure | Pass (B7-3 `MATH-LINEAR-EQUATION` activation) |
| 27 | Structured interactions are versioned, allowlisted, and accessible | `REQ-INTERACTION-01` | `TestB7LinearEquationActivationCompletesFourStagesWithPipelineEvidence` proves released `student-interaction-v1` material reaches the Student DTO with an accessible fallback and no private fields; `TestB7LinearEquationAssistanceRequiresFreshIndependentTaskAndFailsClosedOnDrift` proves renderer drift falls back to text without an attempt | Pass (B7-3 `MATH-LINEAR-EQUATION` structured-content activation) |
| 28 | Learning-effect metrics are computable without answer or audio bodies | `REQ-METRICS-01`, `REQ-METRICS-02` | `learning-effect-event-v1` append-only classifications cover first-answer effective latency with explicit coverage, event-count interaction share, completion by assistance level, transfer accuracy, D+1/D+7 retention, post-EXPLAIN re-engagement, and exit stage. Structured AI outcomes use bounded enums. B6 unit and real PostgreSQL tests prove migration idempotency, body-free schemas, immutable rows, explicit numerators/denominators, unknown-latency exclusion, filters, and Owner rendering | Pass |
| 29 | Identity replacement closes the old learning session without a resume race | `REQ-SESSION-05` | Real PostgreSQL tests `TestB1BLogoutPausesSessionAndPublishesImmediately`, `TestB1BLogoutCutoverIsAtomic`, and `TestB1BLogoutWinsResumeRaceInBothCommitOrders` cover immediate pause, active-time checkpointing, persisted and published realtime events, atomic rollback, revoked-token writes, Parent live state, and both commit orders; frontend auth and StudentLayout tests cover logout-before-login, failed-logout blocking, and cross-tab write shutdown | Pass |
| 30 | Student navigation has three unique destinations without dead redirects | `REQ-NAV-01` | Frontend `Navigation.test.ts` verifies three unique Student destinations with one active link, unchanged four-item Parent and five-item Owner navigation, removed top-level redirects, and retained classroom supply/voice subroutes | Pass |
| 31 | Unknown or cross-knowledge-point misconception codes are rejected and observable without answer data | `REQ-MISCONCEPTION-01` | Real PostgreSQL tests `TestB2MisconceptionCodesAreValidatedAndRejectedCodesAreObservable`, `TestB2RejectedMisconceptionCodesDoNotCreateReviewState`, and `TestB7LinearEquationTaxonomyIsScopedAndDistributed` prove all valid codes persist, rejected codes create only hashed quality events, and no answer-body field or invalid taxonomy/review state is written. Current inventory is 142 RELEASED knowledge points, with 127 having zero misconception associations; future assets for those points can only pass the fail-closed Stage 3 gate with an empty misconception list, so diagnostic and ReviewQueue coverage remains limited until those taxonomies are authored | Pass |

Automated evidence is concentrated in `server/internal/*_test.go`, `server/tests/integration`, and `apps/web/src/*.test.ts`. Provider HTTP mocks exercise the real adapters and metering path, including rejected structured output. No live OpenAI request was made without deployment credentials and a matching Owner-approved price catalog. A real seven-day child trial is also outside engineering proof and remains required before a product-readiness claim.

The automated trial tests must never be cited as proof of child willingness.

The local browser E2E additionally proved role-aware password login and logout, real current-plan session creation, a wrong-answer Tutor transition, Parent child/session discovery without query parameters, private Parent projection, a server-authored encouragement intervention, Owner content/cost/trial rendering, and an empty browser warning/error log. A refreshed `VOICE_EXPLAIN` route recovered a persisted valid audio data URL and synchronized segments, played with the active segment highlighted, and returned through the server to the unchanged original released question. The trial-record and voice-session browser fixtures were manually seeded UI evidence and are explicitly invalid as child-willingness or live-provider evidence. PostgreSQL tests additionally prove real activity-day growth data, support actions without answer-side effects, persisted emotion deescalation, independent-evidence mastery scores, and the complete cross-subject return loop. A separate authenticated HTTP field scan verified that Student session and question responses contain no private answer keys. These are local engineering results, not evidence of live provider quality or child retention.
